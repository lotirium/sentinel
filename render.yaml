services:
  - type: web
    name: codesentinel
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
      # Optional: add TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID
      # in the Render dashboard for live Telegram alerts
